# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

x-filter-all-branches-and-tags: &all-branches-and-tags
  branches:
    only: /.*/
  tags:
    only: /.*/

x-filter-develop-only: &develop-only
  branches:
    only: develop

x-filter-staging-only: &staging-only
  branches:
    only: staging

x-filter-uat-only: &uat-only
  branches:
    only: uat    

x-filter-main-only: &main-only
  branches:
    only: main

x-filter-feature-only: &pushes-and-prs-only
  branches:
    ignore:
      - main
      - develop

restore-dependencies: &restore-dependencies
  restore_cache:
    keys:
      - v1-dependencies1-{{ checksum "package.json" }}
      # fallback to using the latest cache if no exact match is found
      - v1-dependencies1-

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  prep:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - *restore-dependencies
      - run:
          name: Install dependencies
          command: npm install
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies1-{{ checksum "package.json" }}
  unit-tests:
    resource_class: large
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - *restore-dependencies
      - run: npm run test
  build-libs:
    parameters:
      env:
        type: string
        default: development
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - *restore-dependencies
      - run:
          name: Lint
          command: npm run lint
      - run:
          name: Build iframe library
          command: |
            echo "NODE_ENV=$NODE_ENV"
            npm run build-static-iframe-v1 # Build the v1 iframe library.
          environment:
            - NODE_ENV: << parameters.env >>
      - persist_to_workspace:
          root: .
          paths:
            - build/libs
  build-demo:
    parameters:
      env:
        type: string
        default: development
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - *restore-dependencies
      - run:
          name: Build demo
          command: |
            echo "NODE_ENV=$NODE_ENV"
            npm run build-demo # Build the demo site.
          environment:
            - NODE_ENV: << parameters.env >>
      - persist_to_workspace:
          root: .
          paths:
            - build/demo
  deploy-libs:
    parameters:
      domain:
        type: string
      bucket:
        type: string
    docker:
      - image: google/cloud-sdk:282.0.0-alpine
    steps:
      - run: apk add --no-cache git make openssh
      - run: gcloud auth activate-service-account --key-file=<(echo "$GOOGLE_SERVICE_ACCOUNT_KEY")
      - attach_workspace:
          at: .
      - run:
          name: Deploy static files
          command: gsutil -m rsync -r -d build/static/ "gs://<< parameters.bucket >>/static"
  deploy-demo:
    parameters:
      domain:
        type: string
      bucket:
        type: string
    docker:
      - image: google/cloud-sdk:282.0.0-alpine
    steps:
      - run: apk add --no-cache git make openssh
      - run: gcloud auth activate-service-account --key-file=<(echo "$GOOGLE_SERVICE_ACCOUNT_KEY")
      - attach_workspace:
          at: .
      - run:
          name: Deploy demo
          command: gsutil -m rsync -r -d -x 'static' build/demo/tomito/ "gs://<< parameters.bucket >>"

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  default:
    jobs:
      - prep:
          filters: *all-branches-and-tags
      - unit-tests:
          filters: *all-branches-and-tags
          requires:
            - prep
      - build-libs:
          name: build-static-local
          filters: *pushes-and-prs-only
          requires:
            - unit-tests
          env: development
      - build-libs:
          name: build-static-qa
          filters: *develop-only
          requires:
            - unit-tests
          env: qa
      - build-libs:
          name: build-static-staging
          filters: *staging-only
          requires:
            - unit-tests
          env: staging
      - build-libs:
          name: build-static-uat
          filters: *uat-only
          requires:
            - unit-tests
          env: uat                    
      - build-libs:
          name: build-static-prod
          filters: *main-only
          requires:
            - unit-tests
          env: production
      - build-demo:
          name: build-demo-local
          filters: *pushes-and-prs-only
          requires:
            - unit-tests
          env: development
      - build-demo:
          name: build-demo-qa
          filters: *develop-only
          requires:
            - unit-tests
          env: qa
      - build-demo:
          name: build-demo-staging
          filters: *staging-only
          requires:
            - unit-tests
          env: staging
      - build-demo:
          name: build-demo-uat
          filters: *uat-only
          requires:
            - unit-tests
          env: uat                    
      - build-demo:
          name: build-demo-prod
          filters: *main-only
          requires:
            - unit-tests
          env: production
      - deploy-libs:
          name: deploy-static-qa
          domain: protect-lite.qa.bestowlabs.com
          bucket: protect-lite.qa.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-static-qa
          filters: *develop-only
      - deploy-libs:
          name: deploy-static-staging
          domain: protect-lite.staging.bestowlabs.com
          bucket: protect-lite.staging.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-static-qa
          filters: *staging-only
      - deploy-libs:
          name: deploy-static-uat
          domain: protect-lite.uat.bestowlabs.com
          bucket: protect-lite.uat.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-static-uat
          filters: *uat-only                    
      - deploy-libs:
          name: deploy-static-prod
          domain: protect-lite.bestowlabs.com
          bucket: protect-lite.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-static-prod
          filters: *main-only
      - deploy-demo:
          name: deploy-demo-qa
          domain: protect-lite.qa.bestowlabs.com
          bucket: protect-lite.qa.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-demo-qa
          filters: *develop-only
      - deploy-demo:
          name: deploy-demo-staging
          domain: protect-lite.staging.bestowlabs.com
          bucket: protect-lite.staging.bestowlabs.com
          context: gcp-policy-platform-staging
          requires:
            - build-demo-qa
          filters: *staging-only
      - deploy-demo:
          name: deploy-demo-uat
          domain: protect-lite.uat.bestowlabs.com
          bucket: protect-lite.uat.bestowlabs.com
          context: gcp-policy-platform-uat
          requires:
            - build-demo-uat
          filters: *uat-only                    
      - deploy-demo:
          name: deploy-demo-prod
          domain: protect-lite.bestowlabs.com
          bucket: protect-lite.bestowlabs.com
          context: gcp-policy-platform-qa
          requires:
            - build-demo-prod
          filters: *main-only
